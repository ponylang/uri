// in your code this `use` statement would be:
// use "uri"
use "../../uri"

actor Main
  """
  Basic URI library example.

  This example will demonstrate URI parsing, building, and template expansion
  once those features are implemented.
  """
  new create(env: Env) =>
    env.out.print("URI library - basic example")
    env.out.print("Coming soon: URI parsing and manipulation examples")
